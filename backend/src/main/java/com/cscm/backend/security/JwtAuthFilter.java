package com.cscm.backend.security;

// Remplacé par JwtAuthWebFilter (WebFilter réactif pour Spring WebFlux)
// Cette classe est conservée pour éviter les erreurs de compilation sur les imports existants.
// Elle n'est plus un bean Spring (@Component retiré).

@Deprecated
public class JwtAuthFilter {
}
